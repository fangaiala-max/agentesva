<div id="toast-region" class="toast-region" aria-live="polite" aria-atomic="false"></div>

<style>
  .toast-region {
    position: fixed;
    right: 20px;
    bottom: 20px;
    z-index: 120;
    display: flex;
    width: min(380px, calc(100vw - 32px));
    flex-direction: column;
    gap: 10px;
    pointer-events: none;
  }
  :global(.ui-toast) {
    --toast-color: var(--accent);
    display: grid;
    grid-template-columns: auto 1fr auto;
    gap: 12px;
    align-items: center;
    padding: 13px 14px;
    border: 1px solid color-mix(in srgb, var(--toast-color) 55%, var(--line-2));
    border-radius: 10px;
    background: rgba(14, 35, 66, .96);
    color: #fff;
    box-shadow: 0 18px 55px rgba(0, 0, 0, .36);
    backdrop-filter: blur(14px);
    pointer-events: auto;
    animation: toast-in .34s cubic-bezier(.22,.8,.3,1) both;
  }
  :global(.ui-toast[data-leaving]) { animation: toast-out .22s ease both; }
  :global(.ui-toast__icon) {
    display: grid;
    width: 28px;
    height: 28px;
    place-items: center;
    border-radius: 50%;
    background: color-mix(in srgb, var(--toast-color) 18%, transparent);
    color: var(--toast-color);
    font: 700 13px var(--mono);
  }
  :global(.ui-toast__copy strong) { display: block; font: 600 13.5px var(--sans); }
  :global(.ui-toast__copy span) { display: block; margin-top: 2px; color: var(--fg-3); font: 12px/1.4 var(--sans); }
  :global(.ui-toast__close) { padding: 5px; border: 0; background: transparent; color: var(--fg-4); cursor: pointer; }
  :global(.ui-toast__close:hover) { color: #fff; }
  @keyframes toast-in { from { opacity: 0; transform: translateY(12px) scale(.97); } }
  @keyframes toast-out { to { opacity: 0; transform: translateX(18px) scale(.97); } }
  @media (max-width: 600px) { .toast-region { right: 16px; bottom: 16px; left: 16px; width: auto; } }
  @media (prefers-reduced-motion: reduce) { :global(.ui-toast), :global(.ui-toast[data-leaving]) { animation: none; } }
</style>
