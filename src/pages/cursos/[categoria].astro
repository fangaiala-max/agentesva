---
import LearningDirectory from '../../components/shared/LearningDirectory.astro';
import {getCollection} from 'astro:content';
export async function getStaticPaths(){return (await getCollection('cursosCategorias')).map(c=>({params:{categoria:c.id}}));}
---
<LearningDirectory section="courses" category={Astro.params.categoria}/>
