---
import LearningDirectory from '../../components/shared/LearningDirectory.astro';
import {getCollection} from 'astro:content';
export async function getStaticPaths(){return (await getCollection('recursosCategorias')).map(c=>({params:{categoria:c.id}}));}
---
<LearningDirectory section="resources" category={Astro.params.categoria}/>
