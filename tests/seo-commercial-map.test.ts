import{readFileSync}from'node:fs';import{join}from'node:path';import{describe,expect,it}from'vitest';const MAP=readFileSync(join(process.cwd(),'SEO-COMMERCIAL-MAP.md'),'utf8');describe('mapa SEO comercial',()=>{it('define las seis piezas de soporte',()=>{for(const slug of['chatbot-para-pymes','automatizar-whatsapp-empresa','automatizar-seguimiento-de-leads','make-vs-n8n-vs-zapier','cuanto-cuesta-automatizar-un-negocio','procesos-que-conviene-automatizar-primero'])expect(MAP).toContain(`/guias/${slug}/`)});it('asigna contrato SEO a cada pieza',()=>{for(const field of['Intención','Query primaria','CTA','Página madre','Enlaces previstos'])expect(MAP).toContain(field)});it('documenta prevención de canibalización',()=>{expect(MAP).toContain('Prevención de canibalización');expect(MAP).toContain('estudio existente');expect(MAP).toContain('página de precios')});});
