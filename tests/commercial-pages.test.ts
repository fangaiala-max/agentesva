import{readFileSync}from'node:fs';import{join}from'node:path';import{describe,expect,it}from'vitest';const read=(n:string)=>readFileSync(join(process.cwd(),'src/pages',n),'utf8');const prices=read('precios-automatizacion-ia.astro'),methodPage=read('como-trabajamos.astro');describe('precios y proceso',()=>{it('usa cinco niveles canónicos',()=>{for(const k of ['diagnostic','workshop','scoped','integrated','support'])expect(prices).toContain(`SERVICE_OFFER.${k}`);expect(prices).toContain('PRICE_DISCLAIMER')});it('explica seis fases y responsabilidades',()=>{for(const p of ['Diagnóstico','Diseño','Implementación','Pruebas','Puesta en marcha','Optimización'])expect(methodPage).toContain(p);expect(methodPage).toContain('Tu responsabilidad');expect(methodPage).toContain('Nuestra responsabilidad')});it('incluye FAQ, schema y CTA medidos',()=>{for(const page of[prices,methodPage]){expect(page).toContain("'@type':'FAQPage'");expect(page).toContain('breadcrumbList(trail)');expect(page).toContain('data-track-placement="hero"');expect(page).toContain('data-track-placement="final_cta"')}})});
