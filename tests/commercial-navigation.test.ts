import{readFileSync}from'node:fs';import{join}from'node:path';import{describe,expect,it}from'vitest';import{DIAGNOSTICO,NAVEGACION_COMERCIAL}from'../src/data/breadcrumbs';const read=(p:string)=>readFileSync(join(process.cwd(),p),'utf8');describe('navegación comercial',()=>{it('publica las cuatro rutas prioritarias',()=>{expect(NAVEGACION_COMERCIAL.map(x=>x.url)).toEqual(['/servicios','/precios-automatizacion-ia','/como-trabajamos']);expect(DIAGNOSTICO.url).toBe('/diagnostico-automatizacion-ia')});it('reorienta la home sin eliminar el directorio',()=>{const page=read('src/pages/index.astro');expect(page).toContain('Hacer diagnóstico gratuito');expect(page).toContain('data-track-placement="hero"');expect(page).toContain('id="directorio"');expect(page).toContain('/servicios/automatizacion-atencion-cliente/')});});
